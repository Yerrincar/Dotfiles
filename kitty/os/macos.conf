# Prefer Homebrew Bash on macOS so Bash completions and ble.sh run on a modern Bash.
# install.sh writes ~/.config/kitty-local/macos.conf with the correct Bash path for this machine.
shell /opt/homebrew/bin/bash
globinclude ${HOME}/.config/kitty-local/*.conf
