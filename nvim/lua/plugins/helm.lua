return {
  'qvalentin/helm-ls.nvim',
  ft = 'helm',
  opts = {
    action_highlight = {
      enabled = false,
    },
    conceal_templates = {
      enabled = false,
    },
    indent_hints = {
      enabled = false,
    },
  },
}
